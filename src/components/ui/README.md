# `src/components/ui`

Componente generice copiate din shadcn/ui (și, eventual, Magic UI / Aceternity UI).
Sunt cod al proiectului, nu dependențe — se pot modifica liber, dar re-colorează-le
pe paleta din `src/index.css` înainte de folosire.

Folderul e exclus din lint (`.oxlintrc.json`), fiind cod extern.
